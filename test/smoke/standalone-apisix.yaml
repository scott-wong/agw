# Minimal standalone routes for Server-header branding smoke test.
routes:
  - id: branding-probe
    uri: /agw-health
    upstream:
      type: roundrobin
      nodes:
        "127.0.0.1:1981": 1
#END
